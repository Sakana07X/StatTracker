## 1.2.1 升级内容 / What's New in 1.2.1

### 新增 / New

- 组合条件（`requires`）：一个条件可以带多个子条件，全部满足才算达成（AND），用于"多个称号 / 多项统计同时达标"这类复合成就（如万事通）
- 子条件支持两种形式：
  - 字符串 = 引用另一个条件 ID（如 `- "mining_master"`），阈值只维护一份，不会不同步
  - 内联 map = 直接写统计检查（`type / key / operator / value / contains`）
- 与条件权限桥完全透明配合：组合条件达成后同样自动授予/回收 `permission` 配置的权限，CyuTitles 等插件无需改动
- 原版统计回填（`seed.vanilla_stats`，默认开启）：玩家首次进服时把原版统计的历史值抄进对应计数器，组合条件与称号判定使用同一数据源，老玩家也能正确解锁；一次性回填、不重复计数，关闭后完全从安装起算
- 回填覆盖所有原版有对应的统计：挖掘、放置、击杀、钓鱼、附魔、合成（计数 + 已合成集合）、物品拾取（总数 + 分材料）、丢弃总数、跳跃、繁殖、在线时长，以及步行 / 疾跑 / 游泳 / 鞘翅 / 船 / 骑乘距离（原版厘米换算为格）

### 健壮性 / Robustness

- 循环引用保护：条件互相引用时不会死循环（深度上限 16，超限视为不满足）
- 引用的条件 ID 不存在时，整个条件作废并在日志告警，不会静默误判
- 子条件缺 `type` / `key` / `value` 时跳过整个条件并告警，避免配置错误导致误发权限
- YAML 键名大小写不敏感读取，兼容不同加载器行为

### 性能 / Performance

- 子条件在加载时解析为对象，运行时零分配
- AND 短路求值：第一个不满足的子条件立即返回，多数玩家实际求值次数远低于子条件总数
- 原版回填只在玩家首次进服执行一次，只写入非零值，之后零开销
- 保留 1.2.0 的查询不建档优化，离线占位符查询不污染缓存
